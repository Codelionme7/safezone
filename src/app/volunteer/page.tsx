import VolunteerSignup from "@/components/VolunteerSignup";

export const metadata = {
  title: "Become a SafeZone volunteer",
  description:
    "Join your neighbourhood's verified volunteer network. Help find missing children fast — report sightings, search assigned zones, no malicious intent.",
};

export default function VolunteerPage() {
  return <VolunteerSignup />;
}
