import DemoFlow from "@/components/DemoFlow";

export const metadata = {
  title: "SafeZone Demo — Walk through a missing-child alert",
};

export default function DemoPage() {
  return <DemoFlow />;
}
